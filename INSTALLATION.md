# Установка iOS приложения Adal CRM

## Быстрый старт

### 1. Откройте проект в Xcode
```bash
cd /Users/zainab/Desktop/adal/mobile
open AdalCRM.xcodeproj
```

### 2. Настройте URL сервера
В файлах `AuthService.swift` и `APIService.swift` измените URL на ваш сервер:

```swift
// Текущий URL (для локальной разработки)
private let baseURL = "http://127.0.0.1:8000/api"

// Измените на ваш продакшн сервер
private let baseURL = "https://your-server.com/api"
```

### 3. Запустите приложение
1. Выберите симулятор iPhone в Xcode
2. Нажмите ⌘+R или кнопку "Play"
3. Приложение запустится в симуляторе

## Тестирование

### Тестовые пользователи:
- **Директор**: `director` / `password123`
- **Менеджер**: `manager1` / `password123`
- **Курьер**: `courier1` / `password123`
- **Мойщик**: `washer1` / `password123`
- **Помощник мойщика**: `assistant1` / `password123`

### Функции по ролям:

#### Курьер:
- Просмотр всех заказов
- Отметка заказов как "Забранный" (статус assigned → picked_up)
- Отметка заказов как "Доставленный" (статус ready_for_delivery → delivered)

#### Мойщик:
- Просмотр заказов в стирке
- Отметка заказов как "Постиранный" (статус in_washing → washed)

#### Помощник мойщика:
- Просмотр постиранных заказов
- Отметка заказов как "Высушенный и упакованный" (статус washed → dried_and_packed)

## Структура проекта

```
mobile/
├── AdalCRM.xcodeproj/          # Проект Xcode
├── AdalCRM/
│   ├── Models/                 # Модели данных
│   │   ├── User.swift         # Модель пользователя
│   │   └── Order.swift        # Модель заказа
│   ├── Services/              # Сервисы
│   │   ├── AuthService.swift  # Авторизация
│   │   └── APIService.swift   # API клиент
│   ├── Views/                 # Контроллеры экранов
│   │   ├── LoginViewController.swift
│   │   ├── OrdersViewController.swift
│   │   ├── OrderTableViewCell.swift
│   │   ├── OrderDetailViewController.swift
│   │   ├── StatisticsViewController.swift
│   │   └── ProfileViewController.swift
│   ├── Assets.xcassets/       # Ресурсы (логотип)
│   ├── AppDelegate.swift      # Делегат приложения
│   ├── SceneDelegate.swift    # Делегат сцены
│   └── Info.plist            # Конфигурация
└── README.md                  # Документация
```

## Особенности дизайна

### Цветовая схема:
- **Основной**: Оранжевый (#FF8C00)
- **Текст**: Черный (#000000)
- **Фон**: Белый (#FFFFFF)
- **Дополнительный**: Серый (#8E8E93)

### Статусы заказов:
- **🔴 Красные** (не взяты): Создан, Назначен
- **🟢 Зеленые** (в работе): Забран, В стирке, Постиран, Высушен и упакован, Готов к доставке
- **⚫ Черный** (завершено): Доставлен
- **⚪ Серый** (отменено): Отменен

### UI элементы:
- Минималистичный дизайн
- Карточки с тенями
- Системные иконки SF Symbols
- Pull-to-refresh
- Индикаторы загрузки

## Требования

- **iOS**: 13.0+
- **Xcode**: 12.0+
- **Swift**: 5.0+
- **Устройства**: iPhone, iPad

## Возможные проблемы

### 1. Ошибка сети
Если приложение не может подключиться к серверу:
- Проверьте, что Django сервер запущен
- Убедитесь, что URL в `baseURL` правильный
- Для симулятора используйте `127.0.0.1`, для устройства - IP адрес компьютера

### 2. Ошибка авторизации
- Проверьте правильность логина/пароля
- Убедитесь, что пользователь существует в Django admin

### 3. Пустой список заказов
- Создайте тестовые заказы через Django admin
- Проверьте права доступа пользователя

## Развертывание на устройстве

### Для тестирования на реальном устройстве:

1. **Подключите iPhone/iPad к Mac**
2. **Выберите устройство в Xcode**
3. **Настройте Apple Developer Account** (для подписи кода)
4. **Измените baseURL** на IP адрес вашего компьютера:
   ```swift
   private let baseURL = "http://192.168.1.100:8000/api"
   ```
5. **Запустите приложение**

### Для App Store:

1. **Зарегистрируйтесь в Apple Developer Program** ($99/год)
2. **Настройте Bundle ID** в проекте
3. **Создайте App Store Connect запись**
4. **Архивируйте и загрузите** через Xcode

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Xcode Console
2. Убедитесь, что backend сервер работает
3. Проверьте сетевые настройки
4. Обратитесь к документации Django REST API
